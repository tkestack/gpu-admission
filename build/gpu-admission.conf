# --kubeconfig= : The kube config file location, used to support TLS
KUBE_CONFIG=""

# Listen address
GPU_QUOTA_ADMISSION_ADDRESS="--address 127.0.0.1:3456"

# --v=0: log level for V logs
GPU_QUOTA_ADMISSION_LOG_LEVEL="--v=3"

# --logtostderr=true: log to standard error instead of files
GPU_QUOTA_ADMISSION_LOGTOSTDER="--logtostderr=true"

# Add your own!
GPU_QUOTA_ADMISSION_ARGS=""
